[package]
name = "nway-merge-sort"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.8", features = ["derive"] }
rand = "0.9.0-alpha.1"
tokio = { version = "1", features = ["full"] }
